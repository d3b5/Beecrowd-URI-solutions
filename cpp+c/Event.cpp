#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a;
    long long int b;

    while(true)
    {
        cin >> a >> b;
        if(a==0) break;
        cout << a*b << endl;
    }
    return 0;
}